apply plugin: 'com.android.library'

ext.abiCodes = ["armeabi-v7a": 1, "arm64-v8a": 2, "x86": 3, "x86_64": 4]
ext.universalBase = 8000

version = "0.1.10"

android {
    namespace 'is.xyz.mpv'
    compileSdk = 36

    defaultConfig {
        minSdkVersion 23
        targetSdk 36

        versionCode 38
        versionName version

        // Load information about the NDK to use
        def props = new Properties()
        def propfile = new File(rootDir, 'ndk.properties')
        if (propfile.exists())
            props.load(new FileInputStream(propfile))
        ndkVersion = props.getProperty('ndkVersion', '')
        ndkPath = props.getProperty('ndkRoot', '')
    }

    buildFeatures {
        buildConfig = true
    }

    // https://youtrack.jetbrains.com/issue/KT-55947
    // Note: with AGP 9 built-in Kotlin, jvmTarget defaults to
    // android.compileOptions.targetCompatibility, so no kotlinOptions block needed.
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
    implementation 'androidx.appcompat:appcompat:1.7.1'
}

// Maven publishing code removed
